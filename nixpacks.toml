[phases.build]
cmds = [
  "mkdir -p storage/framework/sessions storage/framework/views storage/framework/cache storage/framework/testing storage/logs bootstrap/cache",
  "chmod -R 777 storage bootstrap/cache",
  "composer install --no-dev --optimize-autoloader",
  "npm ci && npm run build"
]

[start]
cmd = "php artisan config:clear && php artisan view:clear && php artisan route:clear && php artisan event:clear && php artisan migrate --force && php artisan storage:link --force && php artisan config:cache && php artisan route:cache && php artisan serve --host=0.0.0.0 --port=${PORT:-8080}"
